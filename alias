echo "alias tml='tmux ls'" | sudo tee -a ~/.bashrc
echo "alias tma='tmux attach -t'" | sudo tee -a ~/.bashrc
echo "alias tmr='tmux rename-session -t'" | sudo tee -a ~/.bashrc
echo "alias tmn='tmux new -s'" | sudo tee -a ~/.bashrc
echo "alias tmk='tmux kill-session -t'" | sudo tee -a ~/.bashrc
